# Data Quality

Data Quality contributes Cove's dedicated video review workspace to the extension manifest. Installing and enabling the extension makes the page available in **Settings → My → Interface → Navigation**, where it can be shown, hidden, and reordered with other extension pages.

The review workspace itself is currently supplied by the compatible Cove host. A later extension phase can move the implementation behind the public frontend runtime without changing the route or saved review identity.
