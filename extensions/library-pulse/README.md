# Library Pulse

This extension provides a configurable, full-width Cove dashboard widget. It uses the authenticated frontend runtime API, host-owned configuration editing, loading and error states, and CSS container queries.

Build a development ZIP from the repository root with `package-midnight-rider-extension --repository . --extension com.midnightrider.library-pulse --configuration Debug` and install the URL it prints through Cove's extension installer.
